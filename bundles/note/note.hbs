<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="{{ meta.charset }}">
    <title>{{ page.title }}</title>
    <meta name="description" content="{{ meta.description }}">
</head>
<body>
    {{> header/header }}
    <div class="note">
        <div class="note__name">{{ name }}</div>
        <div class="note__text">{{ text }}</div>
    </div>

    {{> footer/footer }}
</body>
</html>
